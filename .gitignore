# Python cache / temp
__pycache__/
*.pyc
*.pyo
*.pyd

# Jupyter / VSCode
.ipynb_checkpoints/
.vscode/

# Raw CSV exports (ignored by default)
data/*.csv

# Keep only clean, versioned summaries
!data/lpt_transfers_????-??.csv
!data/topk_netflow_total.csv
!data/topk_netflow_by_exchange.csv
!data/top_netflow_anomalies_z2.csv

# Documentation images
# (we do NOT ignore *.png globally; docs/img/ is already tracked)
# If you need to ignore heavy renders, do it per-folder.
img/previews/

# Ignore all PNG in outputs by default...
outputs/*.png

# ...but keep the key charts used in README
!outputs/lpt_price_ema.png
!outputs/lpt_zscore.png
!outputs/lpt_corr.png
!outputs/lpt_apy.png

# Logs
*.log
# runtime (do not version)
logs/*.log
.agent_memory/*.json

# Optional — ignore auto-generated reports if not needed in history
# docs/agent_report_*.md

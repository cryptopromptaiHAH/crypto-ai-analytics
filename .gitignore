# Python cache / temp
__pycache__/
*.pyc
*.pyo
*.pyd

# Jupyter / VSCode
.ipynb_checkpoints/
.vscode/

# Données CSV brutes (tout data/ ignoré par défaut)
data/*.csv

# Garder les CSV mensuels "propres" et nos synthèses
!data/lpt_transfers_????-??.csv
!data/topk_netflow_total.csv
!data/topk_netflow_by_exchange.csv
!data/top_netflow_anomalies_z2.csv

# Images de docs — garder toutes les PNG sous docs/
# (on n'ignore plus *.png globalement)
# Si besoin d'ignorer des rendus lourds, faire par dossier.
# Logs
*.log
